export enum Role {
  USER = 'USER',
  ADMIN = 'ADMIN',
}

export enum DocumentStatus {
  PENDING = 'PENDING',
  APPROVED = 'APPROVED',
  REJECTED = 'REJECTED',
}

export enum RefundType {
  A = 'A',
  B = 'B',
  C = 'C',
}
